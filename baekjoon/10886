#include <iostream>
using namespace std;
int N;

int main(){
	scanf("%d", &N);
	int y = 0, n = 0;
	for (int i = 0; i < N; i++) {
		int v; scanf("%d", &v);
		v == 0 ? n += 1 : y += 1;
	}
	cout << (n > y ? "Junhee is not cute!" : "Junhee is cute!");
}
